import Card from '../../../components/Card/Card';
import styles from './TeamDetails.module.scss';

const TeamProspectsTab = () => (
  <Card>
    <p className={styles.tabPlaceholder}>Prospects coming soon.</p>
  </Card>
);

export default TeamProspectsTab;
